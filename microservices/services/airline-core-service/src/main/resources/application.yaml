server:
  port: 5005

spring:
  application:
    name: airline-core-service
  datasource:
    url: jdbc:mysql://localhost:3306/airline_core_db
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true