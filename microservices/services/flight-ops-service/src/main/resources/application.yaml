server:
  port: 5006
spring:
  application:
    name: flight-ops-service
  datasource:
    url: jdbc:mysql://localhost:3306/airline_flight_db
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
