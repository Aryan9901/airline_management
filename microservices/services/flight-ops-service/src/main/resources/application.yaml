server:
  port: 5002
spring:
  application:
    name: flight-ops-service
  datasource:
    url: jdbc:mysql://localhost:3306/airline_core_db
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
